// Q8
// Write a program that will output the sum of all of the multiples of four
// between 0 and 5000

// ANSWER: the number you should see in the console is 3127500
let sum = 
for (let num = 1; num <= 5000; num * 4 ) {
    let counter = 0;
    if (num <= 5000) {
        counter = num + counter;
    } else {
        console.log(counter);
    }
}

